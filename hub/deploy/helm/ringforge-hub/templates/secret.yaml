apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.secretName }}
  labels:
    app: ringforge-hub
    release: {{ .Release.Name }}
type: Opaque
# Populate these values when deploying:
#   kubectl create secret generic ringforge-hub-secrets \
#     --from-literal=SECRET_KEY_BASE=... \
#     --from-literal=DATABASE_URL=... \
#     --from-literal=REDIS_URL=... \
#     --from-literal=RELEASE_COOKIE=...
#
# This template creates an empty secret as a placeholder.
# In production, manage secrets via external-secrets, sealed-secrets, or vault.
data: {}
